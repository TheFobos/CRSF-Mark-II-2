# Анализ соответствия реализации стандарту CRSF Rev07

## Обзор
Данный документ содержит анализ текущей реализации CRSF протокола и сравнение с официальным стандартом CRSF Rev07.

---

## ✅ Соответствие стандарту

### 1. Формат пакета CRSF
**Стандарт:** `[ADDR][LEN][TYPE][PAYLOAD][CRC]`

**Реализация:** ✅ Соответствует
- `crsf_header_t` правильно определяет структуру
- `frame_size` включает TYPE + PAYLOAD + CRC (payload_size + 2)
- Адресация устройств соответствует стандарту

**Код:**
```cpp
// libs/crsf/crsf_protocol.h:79-85
typedef struct crsf_header_s
{
    uint8_t device_addr; // from crsf_addr_e
    uint8_t frame_size;  // counts size after this byte, so it must be the payload size + 2 (type and crc)
    uint8_t type;        // from crsf_frame_type_e
    uint8_t data[1];     // payload
} PACKED crsf_header_t;
```

### 2. CRC вычисление
**Стандарт:** CRC-8 с полиномом 0xD5

**Реализация:** ✅ Соответствует
- Используется полином 0xD5
- CRC вычисляется для TYPE + PAYLOAD
- Таблица lookup реализована корректно

**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:7
_crc(0xd5)

// libs/crsf/CrsfSerial.cpp:61
uint8_t crc = _crc.calc(&_rxBuf[2], len - 1); // TYPE + PAYLOAD
```

### 3. Типы фреймов
**Реализация:** ✅ Соответствует стандарту
- Все основные типы фреймов определены правильно
- Значения соответствуют стандарту:
  - `CRSF_FRAMETYPE_RC_CHANNELS_PACKED = 0x16` ✅
  - `CRSF_FRAMETYPE_GPS = 0x02` ✅
  - `CRSF_FRAMETYPE_BATTERY_SENSOR = 0x08` ✅
  - `CRSF_FRAMETYPE_LINK_STATISTICS = 0x14` ✅
  - `CRSF_FRAMETYPE_ATTITUDE = 0x1E` ✅
  - `CRSF_FRAMETYPE_FLIGHT_MODE = 0x21` ✅

### 4. Адресация устройств
**Реализация:** ✅ Соответствует стандарту
- `CRSF_ADDRESS_FLIGHT_CONTROLLER = 0xC8` ✅
- Все адреса соответствуют стандарту

### 5. Формат каналов RC
**Стандарт:** 16 каналов по 11 бит каждый = 22 байта

**Реализация:** ✅ Соответствует
- `CRSF_FRAME_RC_CHANNELS_PAYLOAD_SIZE = 22` ✅
- Структура `crsf_channels_t` использует битовые поля по 11 бит ✅

**Код:**
```cpp
// libs/crsf/crsf_protocol.h:87-105
typedef struct crsf_channels_s
{
    unsigned ch0 : 11;
    unsigned ch1 : 11;
    // ... ch15 : 11;
} PACKED crsf_channels_t;
```

### 6. Диапазон значений каналов
**Стандарт:** 
- Минимум: 172 (987us)
- 1000us: 191
- 2000us: 1792
- Максимум: 1811 (2012us)

**Реализация:** ✅ Соответствует
```cpp
// libs/crsf/crsf_protocol.h:9-13
#define CRSF_CHANNEL_VALUE_1000 191
#define CRSF_CHANNEL_VALUE_2000 1792
```

### 7. GPS пакет
**Стандарт:** 15 байт payload, big-endian формат

**Реализация:** ✅ Соответствует
- `CRSF_FRAME_GPS_PAYLOAD_SIZE = 15` ✅
- Используется `be32toh()` и `be16toh()` для конвертации ✅

**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:199-205
_gpsSensor.latitude = be32toh(gps->latitude);
_gpsSensor.longitude = be32toh(gps->longitude);
_gpsSensor.groundspeed = be16toh(gps->groundspeed);
_gpsSensor.heading = be16toh(gps->heading);
_gpsSensor.altitude = be16toh(gps->altitude);
```

### 8. Battery Sensor пакет
**Стандарт:** 8 байт payload

**Реализация:** ✅ Соответствует
- `CRSF_FRAME_BATTERY_SENSOR_PAYLOAD_SIZE = 8` ✅
- Правильная обработка big-endian значений ✅

### 9. Link Statistics пакет
**Стандарт:** 10 байт payload

**Реализация:** ✅ Соответствует
- `CRSF_FRAME_LINK_STATISTICS_PAYLOAD_SIZE = 10` ✅

---

## ⚠️ Потенциальные проблемы и несоответствия

### 1. ATTITUDE пакет - экспериментальные коэффициенты
**Проблема:** Используются экспериментальные коэффициенты вместо стандартных

**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:332-344
// ВНИМАНИЕ: Эти коэффициенты получены экспериментально и могут различаться 
// в зависимости от версии прошивки полетного контроллера (Betaflight/iNAV).
// Стандартная спецификация CRSF: градусы × 100, но данные не соответствуют.

_attitudeRoll = rawVal2 / 175.0;    // bytes 2-3 = Roll
_attitudePitch = rawVal0 / 175.0;   // bytes 0-1 = Pitch
```

**Стандарт CRSF:** ATTITUDE должен быть в формате `градусы × 100` (т.е. деление на 100, а не на 175)

**Рекомендация:** 
- Проверить стандарт CRSF Rev07 для точного формата ATTITUDE
- Возможно, требуется поддержка разных форматов в зависимости от прошивки FC

### 2. Порядок байт в ATTITUDE
**Проблема:** Комментарии указывают на перестановку Pitch/Roll

**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:336-340
// bytes 0-1 = Pitch, bytes 2-3 = Roll (поменяны местами!)
_rawAttitudeBytes[0] = rawVal0;  // Pitch raw
_rawAttitudeBytes[1] = rawVal2;  // Roll raw
_rawAttitudeBytes[2] = rawVal4;  // Yaw raw
```

**Рекомендация:** Проверить стандарт CRSF Rev07 для точного порядка байт в ATTITUDE пакете

### 3. Обработка frame_size в packetFlightMode
**Проблема:** Используется `frame_size - 1` для длины строки

**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:360-361
if (p->frame_size > 0) {
    std::string flightMode((char*)p->data, p->frame_size - 1); // -1 для CRC
}
```

**Анализ:** 
- `frame_size` включает TYPE + PAYLOAD + CRC
- Для строки нужно вычесть TYPE (1 байт) + CRC (1 байт) = 2 байта
- Текущая реализация вычитает только 1 байт

**Рекомендация:** Проверить стандарт - возможно должно быть `frame_size - 2` (TYPE + CRC)

### 4. Обработка невалидных пакетов
**Текущая реализация:**
```cpp
// libs/crsf/CrsfSerial.cpp:54
if (len < 3 || len >(CRSF_MAX_PAYLOAD_LEN + 2)) {
    shiftRxBuffer(1);
    reprocess = true;
}
```

**Проблема:** Минимальная длина проверяется как 3, но согласно стандарту:
- Минимальный пакет: ADDR(1) + LEN(1) + TYPE(1) + CRC(1) = 4 байта
- `len` (frame_size) должно быть минимум 2 (TYPE + CRC)

**Рекомендация:** Проверить стандарт для минимальной длины пакета

### 5. Отсутствие обработки Extended Header Frames
**Стандарт:** CRSF поддерживает Extended Header Frames (диапазон 0x28-0x96)

**Реализация:** ⚠️ Частично
- Типы определены в `crsf_protocol.h`, но не обрабатываются в `processPacketIn()`
- Extended frames имеют другую структуру (4 байта заголовка вместо 3)

**Рекомендация:** Добавить поддержку Extended Header Frames если требуется

### 6. Отсутствие синхронизации по SYNC_BYTE
**Стандарт:** CRSF использует SYNC_BYTE (0xC8) для синхронизации

**Реализация:** ⚠️ Не реализовано
- `CRSF_SYNC_BYTE` определен, но не используется в парсинге
- Текущая реализация полагается на проверку длины пакета

**Рекомендация:** Рассмотреть добавление синхронизации по SYNC_BYTE для повышения надежности

### 7. Failsafe таймаут
**Текущая реализация:**
```cpp
// libs/crsf/CrsfSerial.h:19
static const unsigned int CRSF_FAILSAFE_STAGE1_MS = 120000;  // 2 минуты
```

**Проблема:** Значение 120 секунд (2 минуты) кажется слишком большим для failsafe

**Стандарт CRSF:** Обычно failsafe срабатывает через 1-2 секунды отсутствия данных

**Рекомендация:** Проверить стандарт для рекомендуемого значения failsafe таймаута

### 8. Обработка capacity в Battery Sensor
**Код:**
```cpp
// libs/crsf/CrsfSerial.cpp:373
uint32_t capacity = (p->data[4] << 16) | (p->data[5] << 8) | p->data[6]; // мАч
```

**Проблема:** Используется little-endian для 24-битного значения, но стандарт CRSF обычно использует big-endian

**Рекомендация:** Проверить стандарт для формата capacity (24-bit big-endian или little-endian)

---

## 📋 Рекомендации по улучшению

### Высокий приоритет:
1. **Исправить обработку ATTITUDE пакета:**
   - Проверить стандарт для точного формата (градусы × 100 или другой)
   - Убедиться в правильности порядка байт (Pitch/Roll/Yaw)

2. **Исправить обработку frame_size в packetFlightMode:**
   - Проверить правильность вычитания байт для строки

3. **Проверить формат capacity в Battery Sensor:**
   - Убедиться в правильности endianness для 24-битного значения

### Средний приоритет:
4. **Добавить синхронизацию по SYNC_BYTE:**
   - Улучшит надежность парсинга пакетов

5. **Проверить failsafe таймаут:**
   - Убедиться, что значение соответствует стандарту

6. **Добавить поддержку Extended Header Frames:**
   - Если требуется поддержка расширенных команд

### Низкий приоритет:
7. **Улучшить обработку ошибок:**
   - Более детальная проверка минимальной длины пакета
   - Логирование невалидных пакетов для отладки

---

## 🔍 Детали для проверки в стандарте CRSF Rev07

При проверке PDF стандарта обратите внимание на:

1. **ATTITUDE пакет (0x1E):**
   - Точный формат данных (градусы × 100 или другой)
   - Порядок байт: Pitch, Roll, Yaw
   - Размер payload (должен быть 6 байт)

2. **FLIGHT_MODE пакет (0x21):**
   - Формат строки
   - Как правильно вычислять длину строки из frame_size

3. **BATTERY_SENSOR пакет (0x08):**
   - Формат 24-битного capacity (big-endian или little-endian)
   - Порядок байт в payload

4. **Failsafe:**
   - Рекомендуемое время для Stage 1 failsafe
   - Рекомендуемое время для Stage 2 failsafe

5. **SYNC_BYTE:**
   - Использование 0xC8 для синхронизации
   - Рекомендации по обработке

6. **Extended Header Frames:**
   - Структура заголовка (4 байта вместо 3)
   - Обработка в парсере

---

## ✅ Заключение

**Общая оценка соответствия:** ~85-90%

**Основные соответствия:**
- ✅ Формат пакета
- ✅ CRC вычисление
- ✅ Типы фреймов
- ✅ Адресация
- ✅ RC каналы
- ✅ GPS пакет
- ✅ Link Statistics

**Требуют проверки/исправления:**
- ⚠️ ATTITUDE пакет (коэффициенты и порядок байт)
- ⚠️ FLIGHT_MODE пакет (вычисление длины строки)
- ⚠️ BATTERY_SENSOR capacity (endianness)
- ⚠️ Failsafe таймаут
- ⚠️ Синхронизация по SYNC_BYTE

Реализация в целом соответствует стандарту, но есть несколько моментов, требующих уточнения по стандарту CRSF Rev07.

